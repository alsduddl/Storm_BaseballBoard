<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="
                http://www.springframework.org/schema/beans
                https://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/security
                https://www.springframework.org/schema/security/spring-security.xsd
                http://www.springframework.org/schema/context
                https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Security 관련 Bean Scan -->
    <context:component-scan base-package="org.kgitbank.spring.user" />

    <http auto-config="true" use-expressions="true">
    	<intercept-url pattern="/" access="permitAll" />
    	<intercept-url pattern="/main/**" access="permitAll" />
    	<intercept-url pattern="/post/detail" access="permitAll" />
    	<intercept-url pattern="/post/list" access="permitAll" />
        <intercept-url pattern="/login" access="permitAll" />
        <intercept-url pattern="/loginDo" access="permitAll" />
        <intercept-url pattern="/user/join" access="permitAll" />
        <intercept-url pattern="/user/userIdCheck" access="permitAll" />
        <intercept-url pattern="/user/nicknameCheck" access="permitAll" />
        <intercept-url pattern="/recommend" access="permitAll" />
        <intercept-url pattern="/post/search" access="permitAll" />
        
        <intercept-url pattern="/like/**" access="hasAnyRole('ADMIN','USER')" />
        <intercept-url pattern="/comment/**" access="hasAnyRole('ADMIN','USER')" />
        
        <intercept-url pattern="/user/**" access="hasRole('USER')" />
        
        <intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />  
        
        
		<!-- 로그인 설정 -->
        <form-login login-page="/login"
                    login-processing-url="/loginDo"
                    username-parameter="userId"
                    password-parameter="password"
                    authentication-failure-url="/login?error=true"
                    authentication-success-handler-ref="customLoginSuccessHandler" />
        <logout logout-url="/logout" logout-success-url="/main?logout=true" />
    </http>

	<!-- 인증 관리자 설정 -->
    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="passwordEncoder" />
        </authentication-provider>
    </authentication-manager>

	<!-- 비밀번호 암호화 -->
    <beans:bean id="passwordEncoder"
                class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

	<!-- 로그인 성공 시 페이지 이동 -->
    <beans:bean id="customLoginSuccessHandler"
                class="org.kgitbank.spring.user.service.CustomLoginSuccessHandler" />

</beans:beans>
